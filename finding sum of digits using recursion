#include <iostream>
#include <stack>

using namespace std;

int sumOfDigits(int num)
{
    if (num==0)
        return 0;
    else
        return (num%10)+sumOfDigits(num/10);
}


int main()

{
    int n;
    cin>>n;

    cout<<sumOfDigits(n);


    return 0;
}
